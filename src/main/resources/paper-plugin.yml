main: "${PACKAGE}.WarpPlugin"
name: "${NAME}"
version: "${VERSION}"
api-version: "1.21"
author: "winlogon"
folia-supported: true
description: "SimpleWarp - A simple /warp plugin"
permissions:
  warp.admin:
    description: Permissions to create, edit and delete warps
    default: op
dependencies:
  server:
    CommandAPI:
      load: BEFORE
      required: true
      join-classpath: true
